# 自然收集图鉴 iOS App PRD

## 1. 产品定位

### 1.1 产品一句话

自然收集图鉴是一款面向普通用户的自然观察与收藏 App。用户可以像收集宝可梦图鉴一样，拍摄生活中的花草、昆虫、鸟类和小动物，通过物种识别生成个人自然图鉴，并用任务、徽章和地图足迹记录自己的自然探索。

### 1.2 产品目标

- 降低普通用户认识自然物种的门槛。
- 把“拍到一个生物”转化为“识别、确认、收藏、回顾”的完整体验。
- 用轻量游戏化机制提升用户持续观察自然的动力。
- 在第一版中建立可靠的图片识别、图鉴收藏、隐私保护和数据删除基础。

### 1.3 核心价值

- **识别价值**：帮助用户快速了解身边动植物可能是什么。
- **收藏价值**：把每一次发现沉淀为个人图鉴卡片。
- **成就价值**：通过每日任务和徽章反馈探索进度。
- **回忆价值**：用时间和模糊位置记录自然发现足迹。

### 1.4 产品边界

第一版 MVP 不是专业科研工具，也不承诺识别结果绝对准确。App 应明确提示“识别结果仅供参考”，并鼓励用户在候选结果中自行确认。

## 2. 用户画像

### 2.1 城市自然好奇者

- 年龄：18-40 岁。
- 场景：通勤、散步、逛公园、露营、旅行时看到花草昆虫。
- 需求：想知道“这是什么”，但不愿意查复杂资料。
- 关注点：识别快、中文名清楚、收藏有成就感。

### 2.2 亲子家庭用户

- 年龄：家长 28-45 岁，儿童 4-12 岁。
- 场景：周末公园、学校自然作业、亲子户外活动。
- 需求：用轻松方式带孩子认识自然。
- 关注点：界面友好、安全提示明确、不鼓励接触危险生物。

### 2.3 入门自然观察爱好者

- 年龄：16-50 岁。
- 场景：观鸟、拍花、拍虫、记录小区或校园物种。
- 需求：保存自己的观察记录，逐渐积累个人图鉴。
- 关注点：学名、分类、时间、位置记录和后续可回顾性。

## 3. 核心流程

### 3.1 首次使用流程

1. 用户打开 App。
2. 展示简短引导：拍摄自然生物、识别候选、加入个人图鉴。
3. 请求必要权限：
   - 相机权限：用于拍照识别。
   - 相册权限：用于选择已有照片。
   - 定位权限：用于记录模糊地图足迹，可跳过。
4. 用户进入首页。

### 3.2 拍照识别流程

1. 用户点击首页主操作按钮“拍摄”。
2. 进入相机拍摄，或选择“从相册选择”。
3. 用户确认图片。
4. App 上传图片进行物种识别。
5. 后端返回 1-3 个候选识别结果。
6. App 展示候选结果，包括中文名、学名、类型、置信度或匹配提示。
7. 用户选择一个候选结果并确认。
8. App 创建图鉴记录，保存用户照片、物种信息、发现时间和模糊位置。
9. 展示加入成功反馈，并更新每日任务和徽章进度。

### 3.3 图鉴查看流程

1. 用户进入“图鉴”页。
2. 查看个人已收集物种卡片列表。
3. 卡片展示中文名、学名、类型、发现时间、用户照片。
4. 用户点击卡片进入详情页。
5. 详情页展示更完整的发现记录、候选确认信息、照片和模糊位置。

### 3.4 每日任务与徽章流程

1. 用户进入首页或任务页。
2. 查看今日任务，例如“完成 1 次识别”“收集 1 种植物”“连续 3 天观察”。
3. 用户完成相关行为后，任务进度自动更新。
4. 达成条件时弹出轻量反馈。
5. 徽章自动发放并在徽章页展示。

### 3.5 地图足迹流程

1. 用户进入“地图”页。
2. App 展示用户发现记录的模糊位置点。
3. 用户点击位置点，可查看该区域内的发现列表。
4. 默认不展示精确坐标，不公开分享位置。

## 4. MVP 功能范围

### 4.1 首页

**目标**：承载主要入口和轻量反馈。

功能范围：

- 主操作入口：拍照识别。
- 次操作入口：从相册选择图片。
- 今日任务进度展示。
- 最近加入图鉴的 1-3 条记录。
- 识别安全提示入口。

### 4.2 图片采集

功能范围：

- 调用系统相机拍摄图片。
- 调用系统相册选择图片。
- 图片上传前展示预览确认。
- 支持重新拍摄或重新选择。
- 上传时展示加载状态。
- 上传失败时允许重试。

权限要求：

- `NSCameraUsageDescription`：说明相机用于拍摄自然生物并进行识别。
- `NSPhotoLibraryUsageDescription` 或适用的照片选择权限说明：说明相册用于选择已有自然照片进行识别。

### 4.3 物种识别

功能范围：

- 上传用户确认的图片到识别服务。
- 识别服务返回 1-3 个候选结果。
- 每个候选结果包含：
  - 中文名
  - 学名
  - 类型
  - 简要说明或匹配依据
  - 置信度或推荐程度
- 用户必须手动确认后才加入图鉴。
- 若识别失败，展示友好错误提示。
- 若识别不确定，允许用户选择“暂不加入”。

物种类型 MVP 枚举：

- 植物
- 昆虫
- 鸟类
- 小动物
- 其他/未知

### 4.4 候选结果确认页

功能范围：

- 展示用户上传照片。
- 展示 1-3 个候选识别结果。
- 支持用户选择一个候选结果。
- 确认按钮：加入个人图鉴。
- 取消按钮：放弃本次识别。
- 明确提示：识别结果仅供参考，请勿食用、触摸或靠近不明生物。

### 4.5 个人图鉴

功能范围：

- 图鉴列表页。
- 图鉴卡片展示：
  - 中文名
  - 学名
  - 类型
  - 发现时间
  - 用户照片
- 支持按类型筛选。
- 支持按发现时间倒序排列。
- 图鉴详情页展示：
  - 用户照片
  - 中文名
  - 学名
  - 类型
  - 发现时间
  - 模糊位置
  - 创建来源：拍照或相册

### 4.6 每日任务

功能范围：

- 展示当天任务列表。
- MVP 任务示例：
  - 今日完成 1 次识别。
  - 今日加入 1 个图鉴。
  - 本周收集 3 种不同类型。
- 任务状态：
  - 未开始
  - 进行中
  - 已完成
- 任务完成后发放经验值或进度，不强制引入复杂等级系统。

### 4.7 徽章系统

功能范围：

- 徽章列表页。
- 徽章状态：未获得、已获得。
- MVP 徽章示例：
  - 初次发现：首次加入图鉴。
  - 植物观察者：收集 5 个植物记录。
  - 昆虫朋友：收集 5 个昆虫记录。
  - 早起观鸟：上午完成一次鸟类记录。
  - 连续探索：连续 3 天完成识别。
- 获得徽章时展示轻量弹窗或 Toast。

### 4.8 地图足迹

功能范围：

- 记录用户发现时的位置，前提是用户授权定位。
- 默认保存和展示模糊位置。
- 模糊策略建议：
  - 不保存精确经纬度到前端展示层。
  - 展示时将位置聚合到大约 500 米至 2 公里范围。
  - 对敏感物种或用户主动关闭定位时，仅显示城市级或不显示位置。
- 地图页展示发现点或区域聚合点。
- 点击区域后展示该区域内图鉴记录列表。

权限要求：

- `NSLocationWhenInUseUsageDescription`：说明定位用于记录自然发现的大致足迹，默认模糊展示。

### 4.9 设置页

功能范围：

- 隐私政策入口。
- 安全提示入口。
- 数据管理入口。
- 删除数据入口。
- 权限管理说明。
- 关于 App。

删除数据要求：

- 用户可以删除单条图鉴记录。
- 用户可以申请或执行清空个人图鉴数据。
- 删除前需要二次确认。
- 删除后应同步删除服务端关联数据，或明确说明删除范围和预计完成时间。

### 4.10 安全提示

安全提示内容至少包括：

- 识别结果仅供参考，不构成专业鉴定。
- 不要触摸、采摘、食用不明植物或菌类。
- 不要靠近、捕捉或惊扰野生动物。
- 不要进入危险区域拍摄。
- 儿童使用时建议由监护人陪同。

## 5. 暂不做的功能

第一版 MVP 暂不包含：

- 社区发布、评论、点赞、关注。
- 用户之间的图鉴交换或交易。
- 实时 AR 捕捉玩法。
- 精细物种百科内容库。
- 专家人工鉴定。
- 离线识别。
- 复杂等级、装备、养成系统。
- 公开排行榜。
- 组队任务。
- 精确轨迹记录。
- 对外公开分享用户位置。
- 商业化订阅、内购、广告。
- 多端同步以外的复杂云盘管理。
- 面向科研用途的数据导出和标注系统。

## 6. 数据结构草案

以下为 MVP 阶段的核心数据结构草案，字段命名仅作产品和研发沟通参考。

### 6.1 User

```json
{
  "id": "string",
  "nickname": "string",
  "avatarUrl": "string",
  "createdAt": "datetime",
  "lastActiveAt": "datetime",
  "privacySettings": {
    "locationEnabled": true,
    "locationPrecision": "blurred",
    "allowAnalytics": false
  }
}
```

### 6.2 Observation

用户的一次自然发现记录。

```json
{
  "id": "string",
  "userId": "string",
  "speciesId": "string",
  "commonNameZh": "string",
  "scientificName": "string",
  "category": "plant | insect | bird | animal | other | unknown",
  "photoUrl": "string",
  "thumbnailUrl": "string",
  "source": "camera | photo_library",
  "observedAt": "datetime",
  "createdAt": "datetime",
  "location": {
    "enabled": true,
    "displayName": "string",
    "blurredLatitude": 31.2304,
    "blurredLongitude": 121.4737,
    "precisionMeters": 1000
  },
  "recognition": {
    "requestId": "string",
    "confirmedCandidateId": "string",
    "confidence": 0.82
  }
}
```

### 6.3 Species

物种基础信息。

```json
{
  "id": "string",
  "commonNameZh": "string",
  "scientificName": "string",
  "category": "plant | insect | bird | animal | other | unknown",
  "descriptionShort": "string",
  "riskHint": "string",
  "createdAt": "datetime",
  "updatedAt": "datetime"
}
```

### 6.4 RecognitionRequest

一次图片识别请求。

```json
{
  "id": "string",
  "userId": "string",
  "imageUrl": "string",
  "status": "pending | succeeded | failed",
  "createdAt": "datetime",
  "completedAt": "datetime",
  "errorCode": "string",
  "candidates": [
    {
      "id": "string",
      "speciesId": "string",
      "commonNameZh": "string",
      "scientificName": "string",
      "category": "plant | insect | bird | animal | other | unknown",
      "confidence": 0.82,
      "reason": "string"
    }
  ]
}
```

### 6.5 DailyTask

每日任务定义。

```json
{
  "id": "string",
  "title": "string",
  "description": "string",
  "taskType": "recognition_count | observation_count | category_collect | streak",
  "targetValue": 1,
  "rewardType": "badge_progress | points",
  "rewardValue": 10,
  "activeDate": "date"
}
```

### 6.6 UserTaskProgress

用户任务进度。

```json
{
  "id": "string",
  "userId": "string",
  "taskId": "string",
  "currentValue": 0,
  "targetValue": 1,
  "status": "not_started | in_progress | completed",
  "completedAt": "datetime"
}
```

### 6.7 Badge

徽章定义。

```json
{
  "id": "string",
  "name": "string",
  "description": "string",
  "iconUrl": "string",
  "conditionType": "first_observation | category_count | streak | time_based",
  "conditionValue": 1
}
```

### 6.8 UserBadge

用户徽章获得记录。

```json
{
  "id": "string",
  "userId": "string",
  "badgeId": "string",
  "status": "locked | unlocked",
  "progressValue": 0,
  "unlockedAt": "datetime"
}
```

## 7. App Store 审核相关注意事项

### 7.1 隐私政策

App 上架前必须提供隐私政策，并在 App Store Connect 和 App 内设置页可访问。隐私政策需说明：

- 收集哪些数据：照片、识别请求、图鉴记录、发现时间、模糊位置等。
- 为什么收集：用于物种识别、图鉴保存、地图足迹和任务徽章。
- 数据是否上传服务器。
- 图片是否用于模型训练或产品改进；如使用，需明确说明并提供选择权。
- 数据保存期限。
- 用户如何删除数据。
- 是否使用第三方识别服务、地图服务、分析服务。

### 7.2 权限说明

iOS 权限弹窗文案必须具体、准确，不得泛泛而谈。

建议文案方向：

- 相机：用于拍摄你发现的花草、昆虫、鸟类和小动物，并进行物种识别。
- 相册：用于选择已有自然照片进行物种识别和加入图鉴。
- 定位：用于记录发现地点的大致足迹，默认以模糊位置展示。

定位权限应允许用户跳过。核心识别和图鉴功能不应强制依赖定位。

### 7.3 用户生成内容与安全

虽然 MVP 暂不做社区，但用户照片属于用户生成内容。需要注意：

- 不公开展示用户照片，除非未来加入分享功能并获得明确授权。
- 避免鼓励用户捕捉、伤害、惊扰野生动物。
- 对可能涉及危险行为的页面加入安全提示。
- 对儿童可能使用的场景保持谨慎，不引导未成年人接触危险生物。

### 7.4 识别准确性声明

物种识别存在误差，尤其是相似物种、幼体、局部照片、模糊照片和有毒植物/动物场景。

产品中应明确：

- 识别结果仅供参考。
- 不用于食用、医疗、毒性判断、宠物诊断或专业鉴定。
- 用户确认是加入个人图鉴的必要步骤。

### 7.5 位置隐私

地图足迹需要特别注意：

- 默认展示模糊位置。
- 不在界面展示精确经纬度。
- 不公开用户位置。
- 对敏感物种、家庭住址附近发现记录，应避免精确展示。
- 设置页提供关闭位置记录的说明或入口。

### 7.6 数据删除

App 应提供清晰的数据删除入口，至少支持：

- 删除单条图鉴记录。
- 删除用户全部图鉴数据。
- 删除账号或注销账号，如果 App 有账号体系。

删除行为应有二次确认，并说明照片、识别记录、位置记录等关联数据是否一并删除。

### 7.7 第三方服务披露

如果使用第三方能力，需要在隐私政策和 App Store 隐私标签中披露：

- 物种识别 API。
- 图片存储服务。
- 地图服务。
- 数据分析或崩溃监控 SDK。

第三方 SDK 不应收集与功能无关的数据，尤其要谨慎处理定位、设备标识符和照片元数据。

### 7.8 照片元数据处理

上传图片前建议处理 EXIF 元数据：

- 默认移除精确 GPS 信息。
- 仅保存产品所需的模糊位置。
- 避免无意上传用户家庭地址、拍摄设备信息等敏感元数据。

### 7.9 儿童与家庭使用场景

若 App 宣传面向儿童或亲子使用，需要额外注意：

- 不使用诱导性付费设计。
- 不收集儿童不必要的个人信息。
- 安全提示要清晰。
- 如选择儿童类别上架，需要满足更严格的儿童隐私和第三方 SDK 要求。

### 7.10 付费与商业化

MVP 暂不做内购。如后续加入订阅或付费功能，需要：

- 使用 Apple In-App Purchase。
- 明确说明付费权益。
- 不将已承诺的免费核心能力突然改为付费而无说明。

## 8. MVP 成功指标

建议第一版关注以下指标：

- 首次识别完成率。
- 候选结果确认率。
- 识别失败率。
- 首次加入图鉴转化率。
- 次日留存率。
- 每日任务完成率。
- 用户关闭定位的比例。
- 删除数据入口使用率和相关反馈。

## 9. 风险与对策

### 9.1 识别不准

风险：错误识别影响信任。

对策：

- 展示多个候选。
- 要求用户确认。
- 明确“仅供参考”。
- 提供“都不是/暂不加入”入口。

### 9.2 用户隐私担忧

风险：照片和位置上传导致用户顾虑。

对策：

- 上传前确认。
- 默认模糊位置。
- 移除 EXIF 精确定位。
- 设置页提供数据删除入口。

### 9.3 游戏化过度

风险：任务和徽章喧宾夺主，影响自然观察的轻松感。

对策：

- MVP 使用轻量任务。
- 不做排行榜和强竞争。
- 徽章作为正反馈，不制造焦虑。

### 9.4 安全行为风险

风险：用户为完成任务接触危险生物或进入危险区域。

对策：

- 安全提示常驻可访问。
- 不设置诱导靠近野生动物的任务。
- 不设置采摘、捕捉、投喂类任务。
